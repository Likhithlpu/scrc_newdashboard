version: "3.9"
services:
    nginx:
        build: .
        volumes:
            - ../../storage:/vol/www/
            - ./default.conf:/etc/nginx/conf.d/default.conf
        restart: always
        network_mode: "host"